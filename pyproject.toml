[project]
name = "SYSU-bigwatermelon"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "pi-dal", email = "hi@pi-dal.com"},
]
dependencies = ["playwright>=1.56.0", "opencv-python-headless>=4.12.0.88", "torch>=2.9.1", "numpy>=2.2.6"]
requires-python = "==3.14.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
